#include <iostream>
#include <cmath>
using namespace std;
int main()
{

  int  j,p,n;
	 double f,h;

	cin >> n;
	cin >> j;
	cin >> p;

	while ((1 > j) && ( j > p) && (p > n))
	{
		cin >> j,p;
	}
	double* m = new double[n];
	for (int i = 0; i < n; i++)
	{
		cout << "enter  " <<i<< " elem. of mass. ";
		cin >> m[i];
		cout << endl;
	}
	f = 0;
	

	for (int i = 0; i < n; i ++)
	{

		cout << m[i] << "\t";

	}

	for (int i = j; i <= p; i ++ )
	{
		f = m[i] + f;
	}

	cout << endl;
	h = f / (p - j + 1);
	cout <<h;

}
