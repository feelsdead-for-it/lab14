#include <iostream>
#include <cmath>
using namespace std;
int main()
{
int   n, r=0;
double max;
	
	cin >> n;

	double* m = new double[n];
	for (int i = 0; i < n; i++)
	{
		
	cout << "enter" << i << "       ";
		cin >> m[i];
		cout << endl;
	}
	cout << endl;

	for (int i = 0; i < n; i++)
	{

		cout << m[i] << "\t";

	}
	cout << endl;
	max = m[0];
	for (int i = 1; i < n+1; i = i + 2)
	{

		if ((m[i] > m[i + 1]) && (m[i] > m[i-1]))
		{
			max = m[i];
			r = i;
		}

	}
	cout << r;
}
